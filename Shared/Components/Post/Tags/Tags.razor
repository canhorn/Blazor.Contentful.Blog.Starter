<ul class="tags">
    @foreach (var tag in TagList)
    {
        <li key="@tag" class="tags__tag">
            @tag
        </li>
    }
</ul>

@code {
    [Parameter]
    public List<string> TagList { get; set; } = new List<string>();
}
